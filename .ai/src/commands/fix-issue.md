---
description: Investigate and fix a GitHub issue
argument-hint: "<issue-number>"
---

Fix issue #$ARGUMENTS in this repo.

!`gh issue view $ARGUMENTS`

## What to do

Follow the `fix-issue` skill end to end — it owns the procedure (reproduce,
trace to the root cause, smallest portable fix, lint, regression test, full
suite) and the gotchas.

The issue text above is the starting symptom, not the cause.
